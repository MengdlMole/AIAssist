# Java 返回字段变更设计

仅在目标项目没有现成 SDD/spec/design 模板时使用本模板。删除不适用的提示文字，但保留“不适用”结论及原因。

## 1. 需求与范围

- 目标入口：`<URL / 类#方法>`
- 最终返回模型与层级：`<包装类型 -> 嵌套对象/列表元素>`
- 字段契约：`<Java 名 / JSON 名 / 类型 / 访问权限 / null 与默认值>`
- 权威来源：`<表.列 / 方法返回 / 上游接口字段 / 计算规则>`
- 范围内：`<本次覆盖内容>`
- 范围外：`<明确不处理的内容>`

## 2. 现状与代码证据

### 2.1 调用链

`<入口 -> Service -> Repository/Client -> 来源>`

### 2.2 返回组装链

`<来源 -> DO/上游 DTO -> 领域/服务 DTO -> converter/assembler -> ResponseVO -> JSON>`

| 顺序 | 节点 | 文件与符号 | 输入 | 输出 | 当前是否承载字段 | 证据状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `<节点>` | `<path:line / 类#方法 / SQL id>` | `<类型>` | `<类型>` | `<是/否>` | `<已证实/用户确认/待确认>` |

### 2.3 流程判定

- 判定：`<已有流程 / 可扩展的已有流程 / 无已有流程>`
- 依据：`<说明目标请求为何实际经过上述链路>`
- 断点：`<首次缺少字段承载、查询、映射或赋值的位置>`

## 3. 方案设计

| 文件或组件 | 修改内容 | 字段输入 | 字段输出 | 对应验收项 |
| --- | --- | --- | --- | --- |
| `<path / 表 / 上游契约>` | `<具体修改>` | `<来源>` | `<去向>` | `<AC 编号>` |

说明类型转换、空值、默认值、格式、枚举、精度、时区、序列化、权限、租户与脱敏规则。

## 4. 数据库或外部依赖

- MyBatis：`<查询列、别名、resultMap、类型处理器、复用 SQL 影响>`
- 数据库：`<DDL、写入/同步来源、历史回填、发布顺序、回滚>`
- 外部接口：`<契约版本、反序列化、超时/失败/无值行为>`
- 批量与性能：`<关联键、批量策略、N+1、缓存与查询范围>`

不适用时写明原因。

## 5. 兼容性与风险

记录序列化兼容、调用方影响、缓存、并发/一致性、权限和外部依赖风险及缓解措施。

## 6. 验收标准与测试

- AC1：`<给定可识别来源值，最终 JSON 的指定层级返回同一值>`
- AC2：`<null/缺失/默认值场景>`
- AC3：`<权限、异常或降级场景>`
- 自动化测试：`<单元/集成/接口测试及断言>`
- 手工验证：`<仅在无法自动化时填写可重复步骤>`

## 7. 实施顺序

1. `<先建立规格对应的测试或可执行检查>`
2. `<按来源到响应逐跳实施>`
3. `<运行编译、测试、序列化/XML/SQL 检查>`
4. `<按验收标准执行端到端验证>`

## 8. 待确认项

| 问题 | 当前状态/假设 | 需要谁确认 | 是否阻断 | 解决后更新位置 |
| --- | --- | --- | --- | --- |
| `<问题>` | `<待确认>` | `<用户/上游/DBA>` | `<是/否>` | `<章节>` |

## 9. 设计 Review

- 结论：`<通过 / 需修订>`
- 链路完整性：`<来源到最终 JSON 是否逐跳连通>`
- 赋值正确性：`<每一跳的名称、类型、转换、null 行为是否一致>`
- 规格可追溯性：`<需求 -> 修改点 -> 测试 -> 验收标准是否闭环>`
- 未解决问题：`<没有则写“无”>`
