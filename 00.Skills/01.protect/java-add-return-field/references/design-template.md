# Java 返回字段变更设计

仅在目标项目没有现成 SDD/spec/design 模板时使用本模板。删除不适用的提示文字，但保留“不适用”结论及原因。

## 1. 需求与范围

- 目标入口：`<URL / 类#方法>`
- 最终返回模型与层级：`<包装类型 -> 嵌套对象/列表元素>`
- 字段契约：`<Java 名 / JSON 名 / 类型 / 访问权限 / null 与默认值>`
- 权威来源：`<表.列 / 方法返回 / 上游接口字段 / 计算规则>`
- 范围内：`<本次覆盖内容>`
- 范围外：`<明确不处理的内容>`

## 2. 现状与代码证据

### 2.1 调用链

`<完整路径：入口 -> Service -> Repository/Client -> 来源；增量路径：入口 -> 基线 Rn-Rm -> 差异节点 Dn -> 来源>`

### 2.2 返回组装链

`<完整路径：来源 -> DO/上游 DTO -> 领域/服务 DTO -> converter/assembler -> ResponseVO -> JSON；增量路径：来源 -> 差异节点 Dn -> 基线 Rn-Rm -> JSON>`

| 顺序 | 节点或基线引用 | 文件与符号 | 输入 | 输出 | 当前是否承载字段 | 证据状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `<完整节点 / 基线 Rn-Rm>` | `<path:line / 类#方法 / SQL id；引用基线时填共享性证据>` | `<类型>` | `<类型>` | `<是/否>` | `<已证实/用户确认/待确认>` |

### 2.3 流程判定

- 判定：`<已有流程 / 可扩展的已有流程 / 无已有流程>`
- 依据：`<说明目标请求为何实际经过上述链路>`
- 断点：`<首次缺少字段承载、查询、映射或赋值的位置>`

### 2.4 参考基线与适用性分级

强或部分参考填写参考基线、共享节点和差异；弱或无参考只保留用户答复、检索范围、分级及其证据，删除不适用的表格。

- 用户答复：`<用户指定的参考字段与定位线索 / 无用户指定参考字段 / 待回复>`
- 检索范围与结论：`<搜索过的字段名、接口、类、方法及候选筛选结论>`
- 选定参考字段：`<字段名、入口与最终响应层级；无可用参考时写明原因>`
- 参考强度：`<强参考 / 部分参考 / 弱参考 / 无参考>`
- 分级依据：`<关键执行路径、权威来源、响应层级、组装机制及业务行为是否一致的代码证据>`
- 参考实现代码链路：`<权威来源 -> 查询/上游 DTO -> DO/entity -> Service DTO -> converter/assembler -> ResponseVO -> JSON>`

| 顺序 | 参考流程节点 | 文件与符号 | 字段输入 | 字段输出 | 实现方式 | 代码证据 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `<节点>` | `<path:line / 类#方法 / SQL id>` | `<来源>` | `<去向>` | `<查询/映射/转换/赋值/序列化>` | `<已读取的代码事实>` |

| 比较维度 | 参考字段 | 目标字段 | 设计结论 |
| --- | --- | --- | --- |
| 来源与调用链 | `<内容>` | `<内容>` | `<复用/调整/不可照搬及理由>` |
| 类型、层级与赋值 | `<内容>` | `<内容>` | `<复用/调整/不可照搬及理由>` |
| 空值、权限、批量与异常 | `<内容>` | `<内容>` | `<复用/调整/不可照搬及理由>` |

- 共享节点：`<参考基线节点编号；无则写“无”>`
- 共享性证明：`<目标入口实际进入共享节点的调用点、条件分支或测试证据>`
- 首次分叉点：`<基线节点编号、文件与符号>`
- 差异节点：`<目标字段需要新增或调整的查询、承载、映射、赋值和序列化节点>`
- 路径选择：`<增量路径 / 部分复用路径 / 完整路径>`

## 3. 方案设计

- 设计模式：`<增量设计 / 完整设计>`
- 目标完整链路：`<强参考时写“基线 R1-Rn -> 差异节点 D1 -> 基线 Rm-Rx -> JSON”；其他情况逐跳写完整链路>`

| 文件或组件 | 修改内容 | 字段输入 | 字段输出 | 参考基线/差异理由 | 对应验收项 |
| --- | --- | --- | --- | --- | --- |
| `<path / 表 / 上游契约>` | `<具体修改>` | `<来源>` | `<去向>` | `<复用的基线节点 / 差异原因>` | `<AC 编号>` |

说明类型转换、空值、默认值、格式、枚举、精度、时区、序列化、权限、租户与脱敏规则。

增量设计只描述相对参考基线的变化，但必须通过节点编号和共享性证据还原目标完整链路。未采用某个参考实现时写明目标链路差异或风险，不能只写“参照某字段实现”。

## 4. 数据库或外部依赖

- MyBatis：`<查询列、别名、resultMap、类型处理器、复用 SQL 影响>`
- 数据库：`<DDL、写入/同步来源、历史回填、发布顺序、回滚>`
- 外部接口：`<契约版本、反序列化、超时/失败/无值行为>`
- 批量与性能：`<关联键、批量策略、N+1、缓存与查询范围>`

不适用时写明原因。

## 5. 兼容性与风险

记录序列化兼容、调用方影响、缓存、并发/一致性、权限和外部依赖风险及缓解措施。

## 6. 验收标准与测试

- AC1：`<给定可识别来源值，最终 JSON 的指定层级返回同一值>`
- AC2：`<null/缺失/默认值场景>`
- AC3：`<权限、异常或降级场景>`
- 自动化测试：`<单元/集成/接口测试及断言>`
- 手工验证：`<仅在无法自动化时填写可重复步骤>`

## 7. 实施顺序

1. `<先建立规格对应的测试或可执行检查>`
2. `<按来源到响应逐跳实施>`
3. `<运行编译、测试、序列化/XML/SQL 检查>`
4. `<按验收标准执行端到端验证>`

## 8. 待确认项

| 问题 | 当前状态/假设 | 需要谁确认 | 是否阻断 | 解决后更新位置 |
| --- | --- | --- | --- | --- |
| `<问题>` | `<待确认>` | `<用户/上游/DBA>` | `<是/否>` | `<章节>` |

## 9. 设计 Review

- 结论：`<通过 / 需修订>`
- 链路完整性：`<来源到最终 JSON 是否逐跳连通>`
- 赋值正确性：`<每一跳的名称、类型、转换、null 行为是否一致>`
- 规格可追溯性：`<需求 -> 修改点 -> 测试 -> 验收标准是否闭环>`
- 参考实现有效性：`<是否已询问用户；参考分级是否有证据；增量路径是否证明目标请求经过共享节点>`
- 增量设计完整性：`<参考基线与差异节点能否连续还原目标字段从来源到最终 JSON 的链路>`
- 未解决问题：`<没有则写“无”>`
